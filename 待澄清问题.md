# 待澄清问题与当前口径

这些问题不阻塞资料清理。后续做页面清单、组件清单、状态机、接口依赖、验收用例时，按下面的优先级处理：

1. `预约管理需求-汇总材料 - 260721.xlsx` 的 `需求` TAB 已写明的，以 `需求` TAB 为准。
2. `需求` TAB 未写明、但项目上下文已经补充过的，以补充背景作为当前项目假设。
3. 两者都没有的，遵循 MVP 原则：不先实现；如确实需要设计预留，给一个推荐方案并标记为后续增强。

| 序号 | 问题 | 当前口径 | 状态 |
|---|---|---|---|
| 1 | 预约类型最终有哪些 | `需求` TAB 的预约类型管理明确包含：供应商到货、调配出库、调配入库、施工领料。`需求` TAB 的预约管理又提到承运商送货，先标记为需求内不一致，不擅自扩展预约类型。 | 待澄清 |
| 2 | PC、e链国网、i国网、移动端分别承载哪些角色 | 已补充明确：e链国网、i国网都是互联网侧用户预约入口；PC 端即物资智慧管理平台 PC 管理端，创建、审批、配置、统计等管理动作均发生在物资智慧管理平台 PC 端。 | 已明确 |
| 3 | 放号审核和预约审核是否两条审批链 | `需求` TAB 已写明：放号由中心管理员审核；预约信息由仓库管理员审核。按两条审批链设计。 | 已明确 |
| 4 | 临时预约的发起和审核链 | `需求` TAB 已写明允许仓库临时创建号段，需注明原因，经中心站管理员审核后办理临时预约。原型中的“用户迟到后发起临时预约”可作为场景补充，但不能覆盖需求口径。 | 待澄清 |
| 5 | 取消预约规则 | `需求` TAB 已写明：已完成预约后，只能在预约号段前一天取消，并记录操作。细节仍需明确“前一天”的截止时间和是否按自然日计算。 | 部分明确 |
| 6 | 过号规则 | `需求` TAB 明确有“过号处理、通知、记录次数、超阈值禁约”。原型使用“迟到/未完结”表达，需统一术语和状态。 | 待澄清 |
| 7 | 禁约名单 | `需求` TAB 只要求超取消/过号阈值后禁止当月预约，未要求独立“禁约名单”管理。MVP 可不单独实现禁约名单；如后续需要，推荐按账号维度落地，并可关联单位用于统计。 | MVP 暂不实现 |
| 8 | 数据权限 | `需求` TAB 支持中心站、管辖仓库、仓库管理员管辖范围等概念。当前建议：系统管理员全省；中心管理员按管辖中心站/仓库；仓库管理员按本仓库；外部用户按本人/本单位。 | 推荐口径 |
| 9 | 统计口径 | `需求` TAB 要求按施工队、供应商、承运商、仓库统计每月/每年取消、过号、临时预约。具体按创建时间、执行时间还是处理时间统计未写明，建议后续按“预约执行时间”作为主统计口径。 | 推荐口径 |
| 10 | 旧 Axure 页面中“采购预约、寄存采购预约、交接单、领料单”与当前预约类型如何映射 | 旧 Axure 只作为历史字段/交互参考，不纳入当前预约类型基线；当前基线以 `需求` TAB 和 `预约/` 原型为准。 | 已明确 |
