# 项目接管判断

## 1. 证据优先级

| 优先级 | 材料 | 判断 |
|---|---|---|
| P0 | `预约管理需求-汇总材料 - 260721.xlsx` | 当前范围基线。包含需求、功能排期、研发排期、差异化分析。 |
| P0 | `基础信息表.xlsx` | 当前角色权限基线。角色、权限边界与新静态原型基本一致。 |
| P1 | `预约/` 新静态原型 | 推荐主原型。页面结构与需求表、权限表高度一致。 |
| P2 | 旧 Axure Web/移动端原型 | 历史交易流程参考。能补充到货、领料、审批、预约确认等交互细节，但模块边界不完整。 |
| P3 | WMS 操作手册、Visio 流程图 | 历史业务背景。用于理解现有 WMS 出入库流程，不直接作为新需求范围。 |
| P4 | `_archive`、锁文件、Axure 运行时 | 归档或运行时噪音。活跃工作资料中应剔除。 |

## 2. 为什么看起来像两个团队做了两套原型

旧 Axure 原型围绕 WMS 已有业务流：预约工作台、预约办理、交接单、领料单、到货预约、领料预约、审批状态。它更像是“围绕现有仓储作业单据补预约动作”的设计。

新静态原型围绕 2026 年预约管理需求：中心站、作业面、预约类型、放号、号段审核、预约办理、预约审核、临时预约、统计、取消管理。它更像是“把预约作为资源调度和管控能力重新建设”的设计。

因此，两套原型不是简单重复，而是不同阶段、不同视角的产物：旧原型偏交易办理，新的偏管理闭环。

## 3. 当前能够自洽的主线

可以形成闭环的业务链路如下：

1. 系统管理员维护中心站、中心站管辖仓库、中心管理员。
2. 仓库管理员维护作业面，并设置作业面最大可承载数量。
3. 仓库管理员按仓库、日期、时段、预约类型配置放号号段。
4. 号段提交中心管理员审核；审核通过后才对预约用户可见。
5. 供应商、承运商、施工队、仓库管理员等按权限发起预约。
6. 仓库管理员审核预约申请，并可完结、标记过号或处理取消。
7. 临时预约作为例外流程，需要填写理由并审核。
8. 系统按角色和数据权限统计取消、过号、临时预约、完结等指标。

这条主线同时得到需求表、权限表、新静态原型支撑，可信度最高。

## 4. 旧原型可用但不能直接照搬的部分

可参考：

- 到货预约、领料预约的移动端步骤。
- 预约确认、预约完成、审批通过、审批拒绝等状态页面。
- 交接单、领料单关联字段和表单布局。
- 旧 WMS 中预约办理和预约记录的字段命名。

不能直接作为当前基线：

- 旧原型缺少中心站管理、作业面管理、预约类型管理、放号管理、号段审核、取消预约阈值、统计分析等当前需求模块。
- 旧原型的业务分类偏 WMS 单据流，不完全覆盖当前需求里的资源调度口径。
- 旧 Axure 导出包包含大量播放器运行时文件，文件数量多但需求信息密度低。

## 5. 需要尽快拍板的缺口

| 问题 | 影响 |
|---|---|
| 预约类型最终口径是什么 | 需求表有供应商到货、调配出库、调配入库、施工领料；权限表增加了承运商送货；差异化分析又提到配送、检测。 |
| e链国网、I国网、移动端分别服务哪些角色 | 功能排期提到三个终端，但当前主原型主要是 PC 管理端。 |
| 放号审核和预约审核的责任人边界 | 放号审核是中心管理员；预约审核看起来是仓库管理员，需要固化。 |
| 取消次数和过号次数阈值如何配置 | 需求有管控规则，但需要明确阈值维度、周期、恢复机制。 |
| 凌晨 12 点自动处理到底是自动完结还是自动过号 | 需求文字存在歧义，直接影响定时任务和考核数据。 |
| 临时预约由谁发起、谁补充、谁审核 | 权限表和需求可以拼出流程，但还需明确例外审批链。 |

## 6. 项目负责人建议

以新静态原型作为当前评审底稿，补一轮“业务类型、角色终端、审核责任、异常规则”的决策表。旧 Axure 原型不要丢，但降级为“字段和历史交互参考”。研发拆解时先按十个主模块拆，不要按旧 Axure 页面名称拆。
